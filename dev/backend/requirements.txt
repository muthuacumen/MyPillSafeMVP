# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.12

# Database (SQLite dev / PostgreSQL prod, code-first via SQLAlchemy + Alembic)
sqlalchemy==2.0.36
aiosqlite==0.20.0
asyncpg==0.30.0
alembic==1.14.0

# Auth & security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# Validation & settings
pydantic==2.10.3
pydantic-settings==2.6.1
email-validator==2.2.0

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.28.0

# MyPillSafe Assistant (Phase 5) -- fuzzy KB retrieval for the public
# project-explainer chatbot, app/services/assistant_kb.py. Lightweight
# (pure Python + optional C extension), unlike the heavier ML deps below,
# so it stays in the core install (render.yaml / every deploy).
rapidfuzz==3.10.1

# NOTE: PaddleOCR, OpenCV, and the Claude SDK are intentionally NOT listed
# here — they're heavy/optional and this file is what `render.yaml` installs
# on every deploy. See requirements-optional.txt to enable Priorities 1B/6/7.
